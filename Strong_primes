import math
a,b = map(int,input().split())
primes = []
def checkIsPrime(n):
	isPrime = True
	for i in range(2,int(math.sqrt(n))+1):
		if(n%i==0):
			isPrime = False
	return isPrime
for i in range(a//2,b+b//2):
	if(checkIsPrime(i)):
		primes.append(i)
for i in range(a,b):
	if(i in primes):
		k = primes.index(i)
		if(k!=0 and k!= len(primes) and 2*primes[k]>primes[k-1]+primes[k+1]):
		    print(i)
